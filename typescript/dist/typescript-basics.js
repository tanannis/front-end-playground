"use strict";
// primitive values
let id = 5;
let firstName = "Kobe";
let isGoodboy = true;
// array
let ids = [1, 2, 3, 4, 5]; // an array of only numbers
let names = ["name", "date", "time"]; // an array of only strings
let arr = [1, true, "hello"];
// any
let x = ""; // x can be reassigned to any type
// Union
let pid; // pid can be a string OR number
pid = "22";
pid = 22;
/* Functions */
// returns a boolean
const isFunc = (a, b, c) => { return true; };
// void means not returning anything or undefined or no specific type
const func = (a, b, c) => { };
